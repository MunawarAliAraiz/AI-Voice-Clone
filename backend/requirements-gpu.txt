# GPU-specific dependencies — install on production PC with NVIDIA GPU
# pip install -r requirements-gpu.txt

# PyTorch with CUDA 12.x
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.4.0
torchaudio>=2.4.0

# TTS Engines
f5-tts>=1.0.0
coqui-tts>=0.25.0

# Audio processing
soundfile>=0.12.0
librosa>=0.10.0
numpy>=1.26.0
scipy>=1.12.0
